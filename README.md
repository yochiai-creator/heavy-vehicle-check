# 野田組 フォークリフト始業前点検 DeltaGenerator表示修正版

管理者コード: 1224

修正内容:
- st.success(...) if ... else st.error(...) を通常の if/else に修正
- 最終判定表示の DeltaGenerator 表示を修正
- 車両登録エラー時の DeltaGenerator 表示を修正
- スマホ上部メニュー版をベースに作成

GitHubにはこのZIP内の3ファイルをルート直置きしてください。
Main file path = streamlit_app.py
